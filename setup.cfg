# flake8 cannot read pyproject.toml, so its configuration lives here. Everything
# else (black, isort, mypy) is configured in pyproject.toml, and pytest in
# pytest.ini. Keep max-line-length in step with [tool.black] line-length.
[flake8]
max-line-length = 100
extend-ignore = E203,W503
